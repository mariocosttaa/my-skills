# nestjs-e2e-tests

Browser E2E with Playwright — POM, selectors, fixtures, authenticated session.

**What it does:** Page Object Model; data-testid > role > label; fixtures for login; e2e structure.

## Quick install

```bash
git clone https://github.com/mariocosttaa/my-agent-skills.git && cp -r my-agent-skills/nestjs-e2e-tests ~/.cursor/skills/
```

→ [See all skills](https://github.com/mariocosttaa/my-agent-skills)
