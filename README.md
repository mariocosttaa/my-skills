# docker

Docker, Dockerfile, docker-compose — .env, no hardcoded values, local vs production overrides.

**What it does:** .env and .env.example; Compose patterns; multi-stage Dockerfile; override files.

## Quick install

```bash
git clone https://github.com/mariocosttaa/my-agent-skills.git && cp -r my-agent-skills/docker ~/.cursor/skills/
```

→ [See all skills](https://github.com/mariocosttaa/my-agent-skills)
