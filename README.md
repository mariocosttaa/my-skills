# gin-workflow

GIN project workflow — Jira + repo, per-requirement planning (requisitos, plan, tasks, overview).

**What it does:** Branch/folder from Jira code; templates for docs; implementation ↔ README linkage; pt-PT for requirements.

## Quick install

```bash
git clone -b gin-workflow https://github.com/mariocosttaa/my-agent-skills.git && cp -r my-agent-skills/gin-workflow ~/.cursor/skills/
```

→ [See all skills](https://github.com/mariocosttaa/my-agent-skills)
