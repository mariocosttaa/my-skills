# nestjs-integration-tests

NestJS integration tests (API + DB) with Supertest, factories, deterministic dates.

**What it does:** test/, *.e2e-spec.ts; factories with overrides; date helper; Supertest, Test.createTestingModule.

## Quick install

```bash
git clone https://github.com/mariocosttaa/my-agent-skills.git && cp -r my-agent-skills/nestjs-integration-tests ~/.cursor/skills/
```

→ [See all skills](https://github.com/mariocosttaa/my-agent-skills)
